package org.example.moneytransferservice.logger;
//интерфейс который определяет один метод который должен быть реализован классом который будет выступать в роли логера
public interface Logger {
    void log(String msg);//метод для формирования и записи сообщения
}

